{{- if .Values.gpg_key }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "maia-dashboard.fullname" . }}-gpg-key
  labels:
    {{- include "maia-dashboard.labels" . | nindent 4 }}
type: Opaque
data:
  gpg.key: {{ .Values.gpg_key | b64enc | quote }}
{{- end }}