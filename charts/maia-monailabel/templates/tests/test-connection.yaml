apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maia-monailabel.fullname" . }}-test-connection"
  labels:
    {{- include "maia-monailabel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maia-monailabel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
