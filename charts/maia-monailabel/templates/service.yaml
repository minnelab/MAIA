apiVersion: v1
kind: Service
metadata:
  name: {{ include "maia-monailabel.fullname" . }}
  labels:
    {{- include "maia-monailabel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "maia-monailabel.selectorLabels" . | nindent 4 }}
