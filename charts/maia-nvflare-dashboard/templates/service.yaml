apiVersion: v1
kind: Service
metadata:
  name: {{ include "maia-nvflare-dashboard.fullname" . }}
  labels:
    {{- include "maia-nvflare-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "maia-nvflare-dashboard.selectorLabels" . | nindent 4 }}
