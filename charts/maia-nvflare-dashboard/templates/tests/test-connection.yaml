apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maia-nvflare-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "maia-nvflare-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maia-nvflare-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
