{{- if .Values.maia_xnat_values }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Release.Name }}-maia-xnat
  namespace: {{ .Values.argo_namespace }}
spec:
  project: {{ .Release.Name }}
  source:
    {{- if .Values.maia_xnat_values.chart_name }}
    chart: {{ .Values.maia_xnat_values.chart_name }}
    {{- end }}
    {{- if .Values.maia_xnat_values.path }}
    path: {{ .Values.maia_xnat_values.path }}
    {{- end }}
    repoURL: {{ .Values.maia_xnat_values.repo_url }}
    targetRevision: {{ .Values.maia_xnat_values.chart_version }}
    helm:
      releaseName: {{ .Release.Name }}-maia-xnat
      valuesObject:
      {{ .Values.maia_xnat_values | toYaml | nindent 8 }}
  destination:
    server: {{ .Values.destination_server }}
    namespace: {{ .Values.namespace }}
{{- end }}