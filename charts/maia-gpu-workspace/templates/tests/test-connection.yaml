apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maia-gpu-workspace.fullname" . }}-test-connection"
  labels:
    {{- include "maia-gpu-workspace.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maia-gpu-workspace.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
