#SPDX-License-Identifier: MIT-0
---
# defaults file for configure_maia_dashboard

# Path to the configuration folder containing env.json and cluster config YAML
config_folder: /opt/maia/config

# Enable automatic ArgoCD application synchronization and deployment restart
auto_sync: true

# ArgoCD port for CLI login
argocd_port: 8080